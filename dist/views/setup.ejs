<!-- <!DOCTYPE html>
<html>
<head>
  <title>Setup</title>
</head>
<body>
  <h1><%= message %></h1>
  <p>Check terminal to scan the QR code</p>
</body>
</html> -->









<!DOCTYPE html>
<html>
<head>
  <title>WhatsApp Setup</title>
  <script src="/socket.io/socket.io.js"></script>
  <style>
    #qr-container { margin: 20px; text-align: center; }
    pre { background: #f4f4f4; padding: 10px; border-radius: 5px; }
  </style>
</head>
<body>
  <h1><%= message %></h1>
  <div id="qr-container"></div>
  <script>
    const socket = io();
    socket.on('qr', qr => {
      const container = document.getElementById('qr-container');
      container.innerHTML = `
        <p>Scan this QR code:</p>
        <pre>${qr}</pre>
        <p>On your phone, go to WhatsApp > Settings > Linked Devices</p>
      `;
    });
  </script>
</body>
</html>